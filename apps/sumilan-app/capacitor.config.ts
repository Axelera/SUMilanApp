import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SUMilanApp',
  webDir: '../../dist/apps/sumilan-app',
  bundledWebRuntime: false,
};

export default config;
